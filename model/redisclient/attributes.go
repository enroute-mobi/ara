package redisclient

// This could be shared in the whole application by creating an ara package or something

const (
	ModelID      = "id"
	ReferentID   = "ReferentId"
	ByReferentID = ReferentID

	Line = "line"
)
